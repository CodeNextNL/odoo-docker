version: '3'
services:
  odoo:
    image: wjriedstra/odoo:12.0-dev
    command: odoo
    ports:
      - 8071:8069
    environment:
      - ODOO_DB_HOST=False
      - ODOO_DB_USER=odoo
      - ODOO_ADMIN_PASSWD=strong_odoo_master_password
    volumes:
      - odoo-data:/var/lib/odoo
    restart: always
volumes:
  odoo-data:
    driver: local